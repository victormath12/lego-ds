import React from 'react';
import ReactDOM from 'react-dom';

const Card = () => {
  return <div style={{ width: 200, height: 200, background: '#eee' }}>Card</div>
};

ReactDOM.render(<Card />, document.getElementById('container'));
